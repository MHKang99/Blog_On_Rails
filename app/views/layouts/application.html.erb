<!DOCTYPE html>
<html>
  <head>
    <title>BlogOnRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav>
  <% if user_signed_in? %>
  <h2 class = "nav-item" ><%= link_to "Blog On Rails", posts_path %></h3>
  <h3 class = "nav-item1" > <%= link_to "New Post", new_post_path %> </h3>
  <h3 class = "nav-item2" > <%= link_to "Home", posts_path %> </h3>
  <% else %>
  <h2 class = "nav-item" ><%= link_to "Blog On Rails", posts_path %></h3>
  <h3 class = "nav-item1" > <%= link_to "New Post", session_new_path %> </h3>
  <h3 class = "nav-item2" > <%= link_to "Home", posts_path %> </h3>
  <% end %>
  
  </nav>
  <%if(user_signed_in?) %>
       <h2>Signed in as <%= link_to "#{current_user.name}" , edit_user_path(current_user.id) %></h2> 
      <%= button_to "Log out", sign_out_path, method: :delete %>
    <% else %>
      <%= link_to 'Sign In Page', session_new_path%>
      <%= link_to 'Sign Up Page', new_user_path%>
    <% end %>
    <%= yield %>
  </body>
</html>
